{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "testbed",
            "cwd": "${workspaceRoot}/bin",
            "valuesFormatting": "parseText"
        }
    ]
}